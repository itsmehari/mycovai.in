---
deployment:
  tasks:
    - export DEPLOYPATH=/home3/metap8ok/mycovai.in
    - /bin/rsync -a --exclude='.git' --exclude='.cursor' ./ $DEPLOYPATH/
    - /bin/bash $DEPLOYPATH/dev-tools/post-deploy-cpanel-cleanup.sh
