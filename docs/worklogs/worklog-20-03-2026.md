# Worklog – 20 March 2026

## MyCovai `/local-events` – plan + PRD implementation pass

- **Legacy `/events/`:** Root `.htaccess` 301 to `/local-events/` (and nested paths).
- **Moderation:** Removed temporary auto-approve from `process-event-covai.php`; submissions stay in queue until admin approves.
- **Posters:** Stored `image_url` now `/uploads/events/...` (web-served from site root).
- **Locality:** `COIMBATORE_LOCALITIES` as `<select>` on post form and listing filters; `normalizeSubmittedLocality()` on save.
- **UX:** Filter panel collapsible on small viewports (`events-covai.css` + Bootstrap collapse); listing cards use `formatEventDateRangeDisplay()`.
- **SEO / brand:** `eventsDefaultOgImageUrl()`, `eventsPublicEventUrl()`, `eventsListingImageAbsolute()`; hub pages (today/weekend/month/category/locality/venue) use `SITE_*` copy and stable canonicals; extra `noindex` when non-page query params appear on hubs.
- **Detail:** Multi-line date range in hero + sidebar; disclaimer when `tickets_url` set; share/JSON-LD/Offer URLs use clean `/local-events/event/{slug}`; map suffix uses Coimbatore.
- **ICS:** `PRODID` MyCovai; `DTEND` uses real `end_datetime` or +2h fallback.
- **Admin:** Duplicate detection on approve (redirect + **Approve anyway** with `duplicate_ack`); manage UI shows similar-count badges.
- **Cron:** `local-events/cron-archive-past-events.php` (CLI) calls `archivePastEventListings()`.
- **Discovery:** `coimbatore-news.php` – “Happening this week” section + `events-this-week-strip.php`; featured widget links use clean event URLs.
- **Content:** `database/seed-covai-events-2026-q1-editorial.sql` (editorial placeholders – verify before live).
- **Docs:** `EVENTS-QUICK-START-ADMIN.md` updated for cron + duplicates; PRD inbox file updated with implementation status.

## Follow-ups (manual)

- Run seed SQL on sandbox, then live after confirmation.
- Google Search Console / Rich Results spot-check on one event URL.
- cPanel cron: daily `php .../local-events/cron-archive-past-events.php`.
